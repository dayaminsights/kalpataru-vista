# Cleanup — 2026-08-30

Repo had grown to ~1GB, mostly abandoned-direction assets. Freed ~630MB. Every deletion
below was verified first by content-hash (or explicit doc reference) against what's
actually served under `..._files/` — see `docs/ASSETS.md` for the surviving inventory and
why each remaining file in `assets/` is still there.

## Deleted

| Path | Size | Why |
|---|---|---|
| `assets/KALPATARU SHOW APARTMENT/` | 583MB (428 stills) | Explicitly flagged as unused in `CLAUDE.md`'s "known rough edges" |
| `assets/hero-src/` (incl. `apartment-sequence/`, 428 frames + `hero-building.webp`) | 55MB | Leftover from the abandoned from-scratch scroll-sequence build (`CLAUDE.md`: "A prior from-scratch build attempt... was abandoned in favor of this reskin approach"). Was git-tracked — removed with `git rm`, staged, not yet committed. |
| `assets/golf course slide show/` | 5.7MB | TIFF + 2 PNGs, wrong resolution (736×492 / 1037×692 / 1225×818) to be the source of any live `golf-course-*.jpg`, not referenced anywhere in `docs/` or `CLAUDE.md`/`ARCHITECTURE.md` |
| `assets/Residential_skyscrapers_stand_in…jpeg` | ~1MB | Hash doesn't match `og-image.jpg`; unreferenced anywhere — an unused generated candidate |
| `probe-check2.html`, `probe2.html` | ~400KB | Scratch output from the `?probe=1` diagnostic route, already matched by `.gitignore`'s `probe*.html` pattern |
| `Camellias Residences.html` + `_files/` | 16MB | Design-reference capture; `.gitignore` already documents this as "not part of the site" — the specific assets actually used (gsap/ScrollTrigger, amenities photo pairs) were already copied into `..._files/` and are tracked there |
| `Kalpataru Vista - Buy 3 & 4 BHK...html` + `_files/` | 21MB | Content-research capture; `.gitignore` already documents this as "content research only, not part of the site" |

## Kept

Everything else in `assets/` — verified as either byte-identical to a currently-served
file, or the documented source of one (logo pipeline, hero photo pipeline, raw videos).
Full breakdown in `docs/ASSETS.md`.

## Recoverable if needed

- `assets/hero-src/` — in git history (was tracked before this cleanup); `git log --diff-filter=D -- assets/hero-src` finds the commit.
- The two reference HTML captures — both are "Save Page As" snapshots of live external
  sites (findrealestate.com and the real Kalpataru Vista site); re-capturable if a future
  rebrand pass needs to diff against them again.
- Untracked deletions (`KALPATARU SHOW APARTMENT`, `golf course slide show`, the stray
  jpeg, probe files) were never in git — not recoverable from this repo.
